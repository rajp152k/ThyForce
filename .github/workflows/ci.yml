name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  polylith-and-tests:
    name: Polylith checks and tests
    runs-on: ubuntu-latest

    env:
      PYTHONPATH: bases:components

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: .python-version

      - name: Install dependencies
        run: uv sync --locked

      - name: Run Polylith workspace check
        run: uv run polhy check

      - name: Run Polylith dependency report
        run: uv run polhy deps

      - name: Run polhy tests
        run: uv run hy tests/thyforce/polhy/test_polhy.hy

      - name: Run spec tests
        run: uv run hy tests/thyforce/spec/test_spec.hy

      - name: Run lsp engine tests
        run: uv run hy tests/thyforce/lsp/engine/test_core.hy

      - name: Run lsp analyzer tests
        run: uv run hy tests/thyforce/lsp/analyzer/test_core.hy

      - name: Run lsp analyzer index tests
        run: uv run hy tests/thyforce/lsp/analyzer/test_index.hy

      - name: Run lsp providers tests
        run: uv run hy tests/thyforce/lsp/providers/test_core.hy

      - name: Run lsp server e2e tests
        run: uv run hy tests/thyforce/lsp/server/test_core.hy
