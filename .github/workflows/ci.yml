name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  polylith-and-tests:
    name: Polylith checks and tests
    runs-on: ubuntu-latest

    env:
      PYTHONPATH: bases:components

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v6
        with:
          enable-cache: true

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version-file: .python-version

      - name: Install dependencies
        run: uv sync --locked

      - name: Run Polylith workspace check
        run: uv run polhy check

      - name: Run Polylith dependency report
        run: uv run polhy deps

      - name: Run polhy tests
        run: uv run hy tests/thyforce/polhy/test_polhy.hy

      - name: Run spec tests
        run: uv run hy tests/thyforce/spec/test_spec.hy

      - name: Run lsp engine tests
        run: uv run hy tests/thyforce/lsp/test_core.hy

      - name: Run analysis tests
        run: uv run hy tests/thyforce/analysis/test_core.hy

      - name: Run analysis index tests
        run: uv run hy tests/thyforce/analysis/test_index.hy
